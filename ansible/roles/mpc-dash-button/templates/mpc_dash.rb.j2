Plugin.create :mpc_dash do
  on_amazon_dash_button_pushed do |interface, mac|
    if mac == '{{ amazon_dash_button_macaddr }}'
      system('/usr/bin/mpc --host {{ mpd.host }} --port {{ mpd.port }} toggle')
    end
  end
end
